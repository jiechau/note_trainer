pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp *.html public/
    - cp *.svg public/
    - cp readme.txt public/
    - cp -r sight_reading bookmark chord_lookup public/
  artifacts:
    paths:
      - public
  only:
    - main
    - master
